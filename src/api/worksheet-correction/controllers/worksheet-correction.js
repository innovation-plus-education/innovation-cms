'use strict';

/**
 *  worksheet-correction controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::worksheet-correction.worksheet-correction');
