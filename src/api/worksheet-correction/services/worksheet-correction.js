'use strict';

/**
 * worksheet-correction service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::worksheet-correction.worksheet-correction');
