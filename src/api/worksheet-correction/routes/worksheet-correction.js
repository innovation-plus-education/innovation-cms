'use strict';

/**
 * worksheet-correction router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::worksheet-correction.worksheet-correction');
